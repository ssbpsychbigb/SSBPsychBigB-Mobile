# Notes only — Metro does not load this file.
# Real API host: src/shared/constants/config.ts → DEV_LAN_IP / ANDROID_USE_EMULATOR

# Physical device (same Wi-Fi as PC):
API_BASE_URL=http://192.168.1.5:5000/api/v1

# Android emulator:
# API_BASE_URL=http://10.0.2.2:5000/api/v1
